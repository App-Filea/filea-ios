# Matchfile

# Git repository URL for storing certificates and provisioning profiles
# Private repository in App-Filea organization for certificates
git_url("git@github.com:App-Filea/filea-certificates.git")

# Storage mode (git, google_cloud, s3, etc.)
storage_mode("git")

# App Identifier
app_identifier(["com.nicolasbarb.filea"])

# Your Apple Developer Team ID
# Found in Apple Developer account
team_id("GFYJNR5373")

# Username (Apple ID email)
username("nicolas.barb.pro@gmail.com")

# Generate profiles for specific platforms
platform("ios")

# Additional configuration
# For readonly access (when you just want to download certificates)
readonly(false)

# Organization: App-Filea
# Updated for organization migration

# Generate only specific certificate types
# type("appstore") # or "development", "adhoc", "enterprise"

# Keychain configuration - using default keychain

# Force recreate if profiles are expired
force_for_new_devices(true)